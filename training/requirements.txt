# Training-specific dependencies (install in separate venv)
# Install Unsloth first: pip install unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git

# Fine-tuning
peft>=0.12.0
trl>=0.12.0
bitsandbytes>=0.44.0
transformers>=4.46.0
datasets>=2.21.0
accelerate>=1.0.0

# Experiment tracking
wandb>=0.18.0

# Export
llama-cpp-python>=0.3.0

# Evaluation
rouge-score>=0.1.2
