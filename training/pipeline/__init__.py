"""Training data generation pipeline.

Subpackages:
- sira_db: hand-curated sira event database (schema + period files)
- generators: dataset generation scripts (one per output .jsonl)
- converters: legacy/external format converters
- staging: stage mixing/splitting + Elasticsearch indexing
- validation: dataset validators
"""
