# Auto-generated from pyproject.toml — core dependencies.
# For development: pip install -e ".[dev]"
# For all extras:  pip install -e ".[all]"

# --- Core ---
fastapi
uvicorn[standard]
chromadb>=0.4.0
requests>=2.28.0
pyyaml>=6.0
pydantic>=2.0
python-jose[cryptography]
passlib[bcrypt]
python-multipart
celery[redis]
redis
httpx
filelock>=3.0
z3-solver>=4.12.0

# --- Optional: embeddings (pip install -e ".[embeddings]") ---
# numpy>=1.24.0
# sentence-transformers>=2.2.0

# --- Optional: transcription (pip install -e ".[transcribe]") ---
# faster-whisper>=1.0.0

# --- Optional: graph visualization (pip install -e ".[graph]") ---
# graphviz>=0.20

# --- Dev (pip install -e ".[dev]") ---
# pytest
# pytest-asyncio
# pytest-cov
# httpx
# ruff
